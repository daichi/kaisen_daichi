%h2 買い物カゴ

%table
  = render :partial => "cart_item", :collection => cart.items

  %tr.total-line
    %td 合計 )
    %td.total-call
      = number_with_delimiter(cart.total_price)
      円

  = form_tag({:action => "empty_cart"}, :remote => true, :class => "order") do
    = submit_tag '買物カゴを空にする'
